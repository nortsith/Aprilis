(function(index){
  var online_users = jQuery('#users'),
      users_frame  = jQuery('.frame');

  index.init = function(){
    index.updates();
  };

  index.updates = function(){

  };

  index.init();
})({});
